export function  eventTracking(name, userId, sku, position, url, price, section) {
  // return window.analytic_tracker.sendEvent(name, userId, sku, {
  //   position,
  //   url,
  //   price,
  //   section,
  // })
}
